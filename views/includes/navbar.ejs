<nav class="main-navbar">
  <div class="navbar-inner">
    
    <div class="navbar-left">
      <a href="/" class="navbar-logo-link">
        <img src="/iamges/RestCasa logo.svg" alt="RestCasa" class="navbar-logo" />
      </a>
    </div>

    <div class="navbar-center">
      <a href="/" class="nav-link active">Home</a>
      <a href="/listings" class="nav-link">All Listings</a>
    </div>

    <div class="navbar-right">
       
   <% if(!currUser) { %>
        <a class="btn btn-secondary" href="/signup">Sign up</a>
        <a class="btn btn-secondary" href="/login">Log in</a>
   <% } %>

   <% if(currUser) { %>
        <a class="btn btn-secondary" href="/logout">Log out</a>
   <% } %>

   <% if(currUser) { %>
       <form action="/listings/addnewlistings" method="get" style="display: flex;">
        <button class="btn btn-primary">Add New Listings</button>
       </form>
   <% } %>
   
</div>

  </div>
</nav>